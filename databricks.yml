# This is a Databricks asset bundle definition for dabs_demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabs_demo
  uuid: 11433686-54cc-4106-b725-e1719af80c13

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3691275713681769.9.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3691275713681769.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/gabriel.sylvain@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/gabriel.sylvain@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: gabriel.sylvain@databricks.com
        level: CAN_MANAGE
